# Validación V16 — Cinematic Video Hero

El vídeo optimizado utiliza H.264, 1920 × 1080, 24 fps, sin audio y con fast-start. Su peso se redujo de aproximadamente 20 MB a 7.8 MB. El navegador confirmó `readyState: 4`, reproducción activa (`paused: false`), `muted: true`, `loop: true` y avance real del tiempo de reproducción.

El texto se mantiene en el tercio izquierdo, protegido por un scrim oscuro horizontal y vertical. El área central —la más luminosa durante la transformación orgánica–digital— queda libre de copy. En reduced motion se oculta el vídeo y permanece el poster estático.

Para la prueba de contraste se fijó el vídeo en el segundo 11, correspondiente a la fase azul más luminosa. El frame quedó pausado únicamente en la sesión de validación del navegador; la implementación mantiene autoplay y loop.

La inspección visual confirmó que el scrim conserva el titular y el cuerpo legibles sobre las formas orgánicas y la transición luminosa. En móvil, el copy mantiene su jerarquía, el vídeo usa un encuadre desplazado hacia el centro visual y la franja SENSE–REASON–ACT ya no aparece.
